interface:
  display_name: "Media Storage Lifecycle"
  short_description: "Choose media storage, transfers, and cleanup"
  default_prompt: "Use $media-storage-lifecycle to keep intermediates near processing and choose explicit publication, retention, and cleanup boundaries."
