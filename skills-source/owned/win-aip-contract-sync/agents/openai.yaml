interface:
  display_name: "WIN AIP Contract Sync"
  short_description: "Sync only contracts the AIP frontend actually consumes."
  default_prompt: "Use $win-aip-contract-sync to identify actual frontend consumers, sync only their WIN contracts, prune unused exports, and validate the affected features."
